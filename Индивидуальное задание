#include "pch.h"
#include <stdio.h>
#include <Windows.h>
#include <iostream>

struct client {
	char  name[30];
	int   num_fone;
	char  date_Begin[10];
	char  date_End[10];
	float debet;
};

struct service {
	char  name_ser[30];
	int   code_ser;
	float tarif_ser;
	char  time_ser[3];
};

struct info_clients {
	int   num_fone;
	int   code_ser;
	char  date_use[20];
	char  time_use[200];
};

int main()
{
	SetConsoleCP(1251);
	SetConsoleOutputCP(1251);
	client client[10];
	service service[10];
	info_clients info_clients[10];


}
