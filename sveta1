#include <iostream>
using namespace std;

int main() {
	int arr[1000], n, a, b, kol;

	cin >> n;
		for (int i = 0; i < n; i++) {
			cin >> arr[i];
		}

		cin >> kol;

		for (int i = 0; i < kol; i++) {
			cin >> a >> b;
			for (int j = a - 1; j < b; j++) { 
				cout << arr[j] << endl;
			}

		}
}
