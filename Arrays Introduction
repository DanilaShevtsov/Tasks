#include <iostream>
using namespace std;

int main() {
	int n, k, i = 0, arr[1000], arr1[1000];

	cin >> n;
	k = n;

	for (i; i < n; i++) {
		cin >> arr[i];
		}

		for (i = 0; i < k; i++) {
			arr1[n] = arr[i];
			--n;
	}
		for (i = 1; i <= k; i++) {
			cout << arr1[i] << " ";
		}
}
